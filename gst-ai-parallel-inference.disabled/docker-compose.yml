version: '3.2'

services:
    gst-ai-parallel-inference:
      image: hub.foundries.io/${FACTORY}/qimsdk-lmp:latest
      restart: always
      network_mode: "host"
      tty: true
      stop_grace_period: 5s
      command: /opt/gst-ai-parallel-inference
      volumes:
        - /opt:/opt
