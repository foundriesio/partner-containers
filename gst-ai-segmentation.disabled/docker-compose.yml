version: '3.2'

services:
    gst-ai-segmentation:
      image: hub.foundries.io/${FACTORY}/qimsdk-lmp:latest
      restart: always
      network_mode: "host"
      tty: true
      stop_grace_period: 5s
      # DLC Model
      # command: /opt/gst/gst-ai-segmentation --ml-framework=1 --model=/opt/gst/deeplabv3_resnet50.dlc --labels=/opt/gst/deeplabv3_resnet50.labels
      # TFlite Model
      command: /opt/gst/gst-ai-segmentation --ml-framework=2 --model=/opt/gst/deeplabv3_resnet50.tflite --labels=/opt/gst/deeplabv3_resnet50.labels
      volumes:
        - /opt:/opt
